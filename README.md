# hex-bareiss-mathlib

Mathlib bridge for `hex-bareiss`: the row-pivoted Bareiss determinant
correctness theorems (`bareiss_eq_det`, `bareiss_eq_mathlib_det`). Published from
the `hex-dev` monorepo; do not edit by hand. See `SPEC/hex-bareiss-mathlib.md`.
